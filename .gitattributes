# Exclude dev-only paths from `git archive` output (release tarballs).
# `.gitea/` holds dev-only CI workflows; the public-facing CI lives in
# `.github/`. `scripts/sync-to-github.sh` strips .gitea/ when pushing
# to the public remote.
/.gitea/              export-ignore
/scripts/             export-ignore
